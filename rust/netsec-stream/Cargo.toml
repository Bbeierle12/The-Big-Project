[package]
name = "netsec-stream"
version = "0.1.0"
edition = "2021"

[lib]
name = "netsec_stream"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
